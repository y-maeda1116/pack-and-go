{
  "name": "pack-and-go",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "同行者・子どもの年齢・行き先・滞在時間から持ち物リストと動線メモを動的生成するスマホ向けSPA",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.0.0",
    "tailwindcss": "^4.0.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.0"
  }
}
